<section class="book-section">
  <div class="book-section-inner">
    <img src="/book_ADI.avif" alt="Your DREAMS Are Good Enough book cover" class="book-cover-img" />
    <div class="book-info">
      <h3>Your DREAMS Are Good Enough</h3>
      <p>Check out the artist's other work — a children's book by Mike Traina</p>
    </div>
    <a href="https://www.amazon.com/Your-DREAMS-Are-Good-Enough/dp/1068673106" class="book-cta" target="_blank" rel="noopener">LEARN MORE ›</a>
  </div>
</section>

<style>
  .book-section {
    border-top: 1.5px solid var(--border-light);
    padding: 40px;
    background: var(--card-bg);
  }

  .book-section-inner {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 24px;
    align-items: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .book-cover-img {
    width: 100px;
    height: 140px;
    object-fit: cover;
    display: block;
    flex-shrink: 0;
  }

  .book-info h3 {
    font-family: var(--font-heading);
    font-size: 14px;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin: 0 0 4px;
    color: var(--white);
  }

  .book-info p {
    font-size: 10px;
    color: rgba(255,255,255,0.5);
    letter-spacing: 1px;
    line-height: 1.6;
    margin: 0;
  }

  .book-cta {
    border: 1.5px solid var(--border-light);
    padding: 10px 24px;
    font-size: 9px;
    letter-spacing: 2px;
    text-transform: uppercase;
    cursor: pointer;
    white-space: nowrap;
    color: var(--white);
    transition: all 0.2s;
  }

  .book-cta:hover {
    background: var(--border-light);
    color: var(--near-black);
  }

  @media (max-width: 768px) {
    .book-section { padding: 32px 24px; }
    .book-section-inner { grid-template-columns: 1fr; text-align: center; justify-items: center; }
    .book-cover-img { width: 80px; height: 110px; }
    .book-info h3 { font-size: 12px; }
    .book-info p { font-size: 9px; }
  }

  @media (max-width: 600px) {
    .book-section { padding: 24px 16px; }
  }
</style>
